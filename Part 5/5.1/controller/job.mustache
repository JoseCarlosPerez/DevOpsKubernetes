apiVersion: batch/v1
kind: Job
metadata:
  name: {{{ job_name }}}
  labels:
    website_url: "{{ website_url }}"
spec:
  template:
    spec:
      containers:
      - name: {{{ container_name }}}
        image: {{{ image }}}
        args: ["{{{ website_url }}}"]
      restartPolicy: Never
